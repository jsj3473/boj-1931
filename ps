n = int(input())


meetingRoomsNumber = 1
meetingRoomsNumberList = []

timeList = [tuple(map(int, input().split())) for _ in range(n)]

endMainTimeList = sorted(timeList, key=lambda x: (x[1], x[0]))

endTime = endMainTimeList[0][1]

for idx in range(1,n):

    candidateStartTimes = endMainTimeList[idx][0]
    if candidateStartTimes >= endTime:
        meetingRoomsNumber+=1
        endTime = endMainTimeList[idx][1]

print(meetingRoomsNumber)

튜플의 두번째 원소, 첫번째 원소 순으로 정렬하는 것이 핵심이다.
그런뒤 빨리 끝나는 것부터 골라주면 된다.
회의 시간의 길이는 문제가 되지 않는다.
